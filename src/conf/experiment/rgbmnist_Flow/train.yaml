# @package _global_

defaults:
- /data/rgbmnist
- /trainer/gpu
- /logger/wandb
- /logger/csv
- /paths/default
- /hydra/default
- model
- sweeps
- meta
- _self_

hydra:
  sweeper:
    params:
      ++data.y_catalog.drop_variables: ${....sweeps.drop_variables.0} 
      +seed: ${....sweeps.seeds.0}

trainer:
  max_epochs: 250
